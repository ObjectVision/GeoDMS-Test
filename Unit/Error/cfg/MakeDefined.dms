container MakeDefined
{
	unit<uint32> unitA: nrofrows = 2
	{
		attribute<.> id;
	}
	unit<uint32> unitB: nrofrows = 3
	{
		attribute<.> id;
	}

	attribute<unitA> att (unitA) := MakeDefined(unitA/id, unitB/id);
	
}