container ErrorDisplay
{
	// open this with Ctrl-D: Does FR_Data FailReason appear directly in the DetailPage or does that require tab-refresh.
	parameter<string> norm   : StorageName = "%projdir%/Norm/ThisFileDoesNotExist.txt", StorageType = "str";
	parameter<string> x := unknown_item;
	parameter<string> y := x;
	unit<uint32> u := unique(y);

}