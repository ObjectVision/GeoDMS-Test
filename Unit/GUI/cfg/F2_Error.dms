container subitem
{
	parameter<int32> a := x;
	unit<uint32> u := unique(a);
	unit<uint32> s := select_with_org_rel(u/values == 3);
	parameter<int32> ss := sum(s/org_rel); 
	parameter<int32> sss := ss/ss;
}