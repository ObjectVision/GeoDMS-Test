container issue_template
{
	container src;
	unit<uint32> srcdomain;
	container instance := templ(srcdomain);
	
	template templ
	{
		// begin case parameters
		unit<uint32> domain;
		// end case parameters
		attribute<srcdomain2> a (srcdomain2) := const(0,srcdomain2);
	}
}